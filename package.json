{
  "name": "nats-adapter",
  "version": "0.0.1",
  "description": "Human-friendly NATS library for Node.js",
  "license": "MIT",
  "main": "build/index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "rimraf ./build && tsc"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "nats": "^2.2.0"
  }
}
